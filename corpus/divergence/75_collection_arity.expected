--- any/all/count need their predicate
any() needs a predicate — truthiness is `xs.any(x => x)`
all() needs a predicate — truthiness is `xs.all(x => x)`
count() needs a predicate — truthiness is `xs.count(x => x)`; `xs.len()` is the length
true false 1
true false true
--- take/drop/chunk need their count, and refuse a second argument
TypeError: take() takes 1 argument(s) but 0 were given
TypeError: take() takes 1 argument(s) but 2 were given
TypeError: take() argument must be int, not 'str'
TypeError: take() argument must be int, not 'bool'
TypeError: drop() takes 1 argument(s) but 0 were given
TypeError: chunk() takes 1 argument(s) but 0 were given
ValueError: chunk() needs a size >= 1
ValueError: take() needs a count >= 0
TypeError: take() argument must be int, not 'bool'
TypeError: take() takes 1 argument(s) but 2 were given
[10, 20]
--- sum takes its start by name
sum() takes no positional arguments — the start is named: `xs.sum(start=n)`
sum() start must not be null — leave `start=` out for the default of 0
sum() got an unexpected keyword argument 'base'
13
--- sort takes the function positionally, and `reverse=` by name
sort() takes exactly 1 argument
sort() needs a function, not 'int'
sort() reverse must be a bool, not 'int'
sort() reverse must be a bool, not 'null'
sort() got an unexpected keyword argument 'key'
--- the cut sorts name their replacement
`sorted` is not in Oro — sort with `xs.sort(f)`: `xs.sort(x => x)` for the elements' own order, `xs.sort(f, reverse=true)` for a stable descending one
`sorted` is not in Oro — sort with `xs.sort(f)`: `xs.sort(x => x)` for the elements' own order, `xs.sort(f, reverse=true)` for a stable descending one
`sort_in_place` is not in Oro — every collection operation returns a new collection, so sorting is `xs.sort(f)` and you rebind: `xs = xs.sort(f)`
`reversed` is spelled `reverse` in Oro — `xs.reverse()`, which returns a new collection (there is no in-place reverse)
a `str` is not a collection in Oro — `s.to_list()` is the bridge into the collection protocol, so write `s.to_list().sort(x => x)`
--- a __lt__ that raises during a sort propagates; the original is untouched
boom-lt
[Boom(2), Boom(1)]
--- a key that is not orderable against the others
'<' not supported between instances of 'str' and 'int'
[1, 'a', 2]
