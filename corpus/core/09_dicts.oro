d = {"a": 1, "b": 2}
d["c"] = 3
print(len(d))
print(d["a"])
print(d.get("zz"))
counts = {}
for _, w in ["x", "y", "x", "z", "x"]:
    if w in counts:
        counts[w] = counts[w] + 1
    else:
        counts[w] = 1
# The three `sorted(...)` lines are in
# corpus/divergence/71_collection_methods.oro: the builtin is cut and
# `d.keys().sort_by(k => k)` is not Python.
print(counts["x"])
