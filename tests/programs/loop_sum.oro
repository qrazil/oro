# Summing a list with a for loop, plus the sum() builtin as a cross-check.
nums = [3, 1, 4, 1, 5, 9, 2, 6]
total = 0
for n in nums:
    total = total + n
print(total)
print(sum(nums))
print(min(nums), max(nums))
print(len(nums))
