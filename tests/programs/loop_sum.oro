# Summing a list with a for loop, plus `xs.sum()` as a cross-check.
nums = [3, 1, 4, 1, 5, 9, 2, 6]
total = 0
for _, n in nums:
    total = total + n
print(total)
print(nums.sum())
print(nums.min(), nums.max())
print(len(nums))
