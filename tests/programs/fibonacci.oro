# Recursive Fibonacci: exercises def, calls, recursion, if/return, and the
# forward reference of `fib` to itself.
def fib(n):
    if n < 2:
        return n
    return fib(n - 1) + fib(n - 2)

for _, i in range(16):
    print(fib(i))
